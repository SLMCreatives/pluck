# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

All commands run from the `pluck/` subdirectory (the Next.js app root):

```bash
cd pluck

pnpm dev        # Start dev server
pnpm build      # Production build
pnpm start      # Run production server
pnpm lint       # ESLint
```

There are no tests configured yet.

## Project Structure

```
pluck/               # Next.js app root
  app/
    page.tsx         # Landing/marketing page (all components inlined)
    layout.tsx       # Root layout with ConvexClientProvider
    ConvexClientProvider.tsx
    startup/page.tsx       # Portfolio wizard (multi-step builder, new users)
    dashboard/page.tsx     # Logged-in home: profile summary, stats, phone preview
    dashboard/edit/page.tsx # Edit wizard pre-populated from existing profile
    [user.id]/page.tsx     # Public profile page
    db_data/page.tsx       # Dev data viewer
  components/
    wizard/          # Step components for the builder flow
    ui/              # shadcn/ui primitives (Button, Input, Card, etc.)
    phone-mockup.tsx # Live preview rendered as a phone frame
    portfolio-preview.tsx
  convex/            # Backend: Convex DB queries/mutations
    tasks.ts
    profiles.ts
    _generated/      # Auto-generated by Convex CLI (do not edit)
  types/
    portfolio.ts     # Core data types
```

## Architecture

**Pluck** is a mobile-first portfolio builder. Users fill out a wizard, see a live phone preview, then publish.

### Data model (`types/portfolio.ts`)
- `PortfolioData` — top-level: name, title, bio, profile image, social links, tabs
- `Tab` — named section (e.g. "Work") containing an ordered list of `ContentBlock`
- `ContentBlock` — discriminated union: `GalleryBlock | VideoBlock | ExperienceBlock`

### Wizard flow (`app/startup/page.tsx`)
State machine with steps: `onboarding -> social -> tabs -> blocks -> block-form -> preview`. All state is local React state (`useState`). Each step is a component in `components/wizard/`.

### Backend (Convex)
- `NEXT_PUBLIC_CONVEX_URL` env var required — set in `.env.local`
- `ConvexClientProvider` wraps the app at the root layout
- `convex/profiles.ts` and `convex/tasks.ts` expose `get` queries
- Run `npx convex dev` (alongside `pnpm dev`) to sync schema and functions

### Auth (Convex Auth)
- `@convex-dev/auth` with Password provider
- `convex/auth.ts` — exports `signIn`, `signOut`, `currentUser`
- `middleware.ts` — protects `/startup` and `/dashboard`, redirects unauthenticated users to `/auth`
- `app/auth/page.tsx` — combined sign in / sign up page
- Requires `JWT_PRIVATE_KEY` and `SITE_URL` set in the Convex dashboard env vars

### Styling
- Tailwind CSS v4 with `@tailwindcss/postcss`
- shadcn/ui component library (Radix UI primitives)
- Dark theme throughout; zinc/indigo/emerald palette
- The landing page (`app/page.tsx`) defines all its UI components locally rather than using shared components

## Remaining To-Do

- [x] **Image uploads** — UploadThing (`@uploadthing/react`); file router at `app/api/uploadthing/core.ts`; helpers at `lib/uploadthing.ts`; endpoints: `galleryImage`, `profileImage`, `companyLogo`. Requires `UPLOADTHING_TOKEN` env var.
- [x] **Profile editing** — `app/dashboard/edit/page.tsx` reuses wizard steps pre-populated from `getMyProfile`; `updateProfile` mutation in `convex/profiles.ts` patches existing record with slug uniqueness re-check.
- [x] **Dashboard** — `app/dashboard/page.tsx`: profile card, stats tiles, public URL copy button, live phone preview, links to edit and view public profile. Protected by middleware. Redirects to `/startup` if no profile exists.
- [x] **Publishing flow** — Stripe Checkout payment gate (RM 10) before publish. Flow: wizard → `createPendingProfile` (published: false) → Stripe Checkout session → payment → Stripe POSTs to `CONVEX_SITE_URL/stripe/webhook` (HMAC-SHA256 verified) → `publishByBillId` sets published: true → user lands on `app/startup/paid/page.tsx` which subscribes to `getPublishStatus` and auto-redirects to `/dashboard`. Requires `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` in Convex env vars. Webhook endpoint to register in Stripe dashboard: `{CONVEX_SITE_URL}/stripe/webhook`, event: `checkout.session.completed`.
- [ ] **Public profile polish** — wire "Contact Me" button to WhatsApp/email using `socialLinks`; custom domain support.
